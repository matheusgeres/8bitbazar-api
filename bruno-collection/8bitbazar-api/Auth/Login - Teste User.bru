meta {
  name: Login - Teste User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "teste@teste.com",
    "password": "Senha@123"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token", res.body.accessToken);
  }
}
